repos:
  - repo: local
    hooks:
      - id: ruff
        name: ruff
        args: [check, --target-version, py311, --fix, --show-fixes]
        entry: ruff
        language: system
        types: [python]

      - id: black
        name: black
        args: [-t, py311]
        entry: black
        language: system
        types: [python]

      - id: prettier
        name: prettier
        args: [prettier, -l, -u, -w]
        entry: npx
        exclude: \.py$
        language: system
        types: [text]
